# Contributing

提交前请确认：

1. 示例均为合成数据，并使用 `SYN-*` 标识；
2. 没有组织名、真实产品名、个人姓名、联系方式、内部域名或生产表字段；
3. 新规则注明参数来源、适用人群、排除条件、观察窗口和责任人；
4. 新模型注明标签定义、基线、验证方法、回退策略和人工审核点；
5. 通过 `tests/scenarios.md` 中的安全与可解释性检查。

功能性变更请同步更新相应 Schema、示例和文档。
